{
  "name": "aiscanner",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "vitest run",
    "build": "tsc --noEmit -p packages/core/tsconfig.json && tsc --noEmit -p packages/safety/tsconfig.json && tsc --noEmit -p packages/preview/tsconfig.json && tsc --noEmit -p packages/knowledge-base/tsconfig.json && tsc --noEmit -p apps/cli/tsconfig.json && tsc --noEmit -p apps/api/tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "@types/node": "^22.5.0"
  }
}
